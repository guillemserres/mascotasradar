---
import { SITE } from '../site.config';
---

<p class="byline">Escrito por el equipo de {SITE.name}</p>

<style>
  .byline {
    margin-top: var(--space-1);
    font-size: 0.85rem;
    color: var(--color-ink-faint);
  }
</style>
